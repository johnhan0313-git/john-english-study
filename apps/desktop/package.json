{
  "name": "@sceneenglish/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:web": "npm run build -w @sceneenglish/shell",
    "tauri": "PATH=\"$HOME/.cargo/bin:$PATH\" tauri",
    "dev": "npm run build:web && npm run tauri -- dev",
    "build": "npm run build:web && npm run tauri -- build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.0"
  }
}
